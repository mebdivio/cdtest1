# This workflow will deploy into Divio Live environment on a push event to the master branch

name: master branch, Live Deployment
env:
  API_TOKEN: ${{ secrets.API_TOKEN }}
  PROJECT_SLUG: ${{ secrets.PROJECT_SLUG }}

on:
 push:
  branches: [ master ]

# jobs run to execute the workflow
jobs:

# This workflow contains a single job called "deploy" and runs on ubuntu-latest
 deploy:
  runs-on: ubuntu-latest
  # Steps represent a sequence of tasks that will be executed as part of the job
  steps:
  # Checks-out the repository under $GITHUB_WORKSPACE, so this job can access it
  - uses: actions/checkout@v2
  # Use a script to get the necessary credentials, deploy and broadcast the success of the deployment
  - name: Deploy and report the success of the deployment
    run: | 
      chmod +x "${GITHUB_WORKSPACE}/cd-testlive.sh"
      ./cd-testlive.sh
      